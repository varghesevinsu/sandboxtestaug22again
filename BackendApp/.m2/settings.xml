<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
                    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                    xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                      http://maven.apache.org/xsd/settings-1.0.0.xsd">
	<servers>
		<server>
			<id>central</id>
			<username>${env.ARTIFACTORY_USER}</username>
			<password>${env.ARTIFACTORY_PASS}</password>
		</server>
		<server>
			<id>snapshots</id>
			<username>${env.ARTIFACTORY_USER}</username>
			<password>${env.ARTIFACTORY_PASS}</password>
		</server>
	</servers>
	<pluginGroups>
        <pluginGroup>org.sonarsource.scanner.maven</pluginGroup>
    </pluginGroups>
	<profiles>
		<profile>
		  <repositories>
			<repository>
			  <snapshots>
				<enabled>false</enabled>
			  </snapshots>
			  <id>central</id>
			  <name>${env.MAVEN_RELEASE_REPO_KEY}</name>
			  <url>${env.ARTIFACTORY_URL}/${env.MAVEN_RELEASE_REPO_KEY}</url>
			</repository>
			<repository>
			  <snapshots />
			  <id>snapshots</id>
			  <name>${env.MAVEN_SNAPSHOT_REPO_KEY}</name>
			  <url>${env.ARTIFACTORY_URL}/${env.MAVEN_SNAPSHOT_REPO_KEY}</url>
			</repository>
		  </repositories>
		  <pluginRepositories>
			<pluginRepository>
			  <snapshots>
				<enabled>false</enabled>
			  </snapshots>
			  <id>central</id>
			  <name>${env.MAVEN_RELEASE_REPO_KEY}</name>
			  <url>${env.ARTIFACTORY_URL}/${env.MAVEN_RELEASE_REPO_KEY}</url>
			</pluginRepository>
			<pluginRepository>
			  <snapshots />
			  <id>snapshots</id>
			  <name>${env.MAVEN_SNAPSHOT_REPO_KEY}</name>
			  <url>${env.ARTIFACTORY_URL}/${env.MAVEN_SNAPSHOT_REPO_KEY}</url>
			</pluginRepository>
		  </pluginRepositories>
		  <properties>
			<sonar.host.url>${env.SONAR_HOST_URL}</sonar.host.url>
			<sonar.login>${env.SONAR_AUTH_TOKEN}</sonar.login>
		  </properties>
		  <id>artifactory</id>
		</profile>
	</profiles>
	<activeProfiles>
		<activeProfile>artifactory</activeProfile>
	</activeProfiles>
</settings>
